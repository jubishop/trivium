#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"

echo "Building MCP server..."
"$SCRIPT_DIR/build-mcp"
echo
echo "Building app..."
"$SCRIPT_DIR/build-app"
